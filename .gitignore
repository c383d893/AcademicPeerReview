# R session cruft
.Rhistory
.RData
.Rapp.history
.Rproj.user/
*.Rproj

# OS
.DS_Store

# renv library (lockfile is tracked; the library is not)
renv/library/
renv/staging/

# Intermediate analysis tables — regenerated by scripts 01-04 from the data.
# The precomputed duplicate-title summary is a needed input, so it is kept.
/tables/*.csv
!/tables/duplicate_sensitivity.csv
